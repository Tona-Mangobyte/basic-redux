import React from "react";
import ProductsData from "../../containers/ProductsData";

const ProductList = (props) => {
    return (
        <div>
            <h3>Product List</h3>
            <ProductsData />
        </div>
    );
};

export default ProductList;
